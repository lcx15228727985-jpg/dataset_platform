# 后端（Docker 或本机）
# Linux 下 Docker 内用 db:5432，本机连 Docker 用 localhost:5432
DATABASE_URL=postgresql://admin:password123@db:5432/annotation_system
# 数据集根目录（run/episode/images 结构）。Docker 内一般为 /data/dataset
DATA_ROOT=/data/dataset

# Docker Compose 挂载：宿主机数据集路径，替换默认匿名卷。例: /home/user/dataset
# DATASET_HOST_PATH=/path/on/host/dataset

# 前端构建时 API 地址（仅 build 时生效）。留空表示同源（与前端同域名）
# 若前端与后端同机部署且 Nginx 反代到 /api，可留空
# VITE_API_URL=
